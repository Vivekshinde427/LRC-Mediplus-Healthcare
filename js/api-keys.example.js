// Example API Keys configuration
// Rename this file to api-keys.js and replace with your actual keys
// api-keys.js is ignored by git so your secrets won't be exposed

window.APP_CONFIG = {
    GEMINI_API_KEY: "YOUR_GEMINI_API_KEY_HERE"
};
